package routing

import routing.HttpMethod.{GET, POST}
import PathSegment._

/*
def dummyHandler(request: HttpRequest, params: Seq[String]): HttpResponse = ???

val route: Seq[Route] =
  ("root" / "user" / Variable / "values") (
    POST -> dummyHandler,
    GET -> dummyHandler
  )

*/
